<h2><%= t :invitation_list %></h2>

</ul>
<% @invitations.each do |invitation| %>  
  <li><%= invitation.email %> <small>(valid until <%= l invitation.expiration %>)</li>
<% end %>
</ul>

<%= link_to t(:back), users_path %>
