<%= dm_card title: t(:invitation_list) do %>  
  <ul>
  <% @invitations.each do |invitation| %>  
    <li><%= invitation.email %> <small>(valid until <%= l invitation.expiration %>)</small></li>
  <% end %>
  </ul>

  <p><%= link_to t(:back), users_path %></p>
<% end %>
